# Каталог достижений (аудит)

Машиночитаемые снимки:

- `AchievementsCatalogRaw.csv` — все найденные определения до дедупликации;
- `AchievementsCatalogUnique.csv` — уникальные по нормализованному имени.

## Сводка

| Метрика | Значение |
| --- | --- |
| Найдено до дедупликации | 510 |
| Уникальных после name-dedup | 344 |
| Перенесено в SS14 (прототипы) | **501** |
| Из них Fish-original / креативные | batch03+batch04 |
| Адаптировано из SS13 каталога | batch01+batch02 (+seed) |
| Исключено | 0 (rewards/BeeCoin/role weighting не переносились как награды) |

Многие записи batch02–04 пока с `condition: manual` или общими ключами — точечные handlers добавляются постепенно. Игровых наград нет.
