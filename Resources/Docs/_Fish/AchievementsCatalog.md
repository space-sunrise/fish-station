# Каталог достижений (аудит)

Машиночитаемые снимки:

- `AchievementsCatalogRaw.csv` — все найденные определения до дедупликации;
- `AchievementsCatalogUnique.csv` — уникальные по нормализованному имени.

## Сводка (2026-08-21)

| Метрика | Значение |
| --- | --- |
| Найдено до дедупликации | 510 |
| Уникальных после name-dedup | 344 |
| Перенесено в SS14 (прототипы) | 125 (5 seed + 120 batch01) |
| Адаптировано | 125 |
| Исключено | 0 |

Полные handlers пока покрывают seed-семейства; batch01 в основном зарегистрирован как контент с condition keys (часть `manual` до точечных handlers).

