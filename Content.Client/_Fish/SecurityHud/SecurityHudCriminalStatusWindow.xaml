<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'security-hud-criminal-status-window-title'}"
                      MinSize="360 180"
                      SetSize="400 220">
    <BoxContainer Orientation="Vertical" Margin="8" VerticalExpand="True">
        <Label Name="PersonName" StyleClasses="LabelBig" Margin="0 0 0 4" />
        <BoxContainer Orientation="Horizontal" Margin="0 0 0 8">
            <Label Text="{Loc 'crew-monitoring-ui-job-label'}" FontColorOverride="DarkGray" />
            <Label Name="PersonJob" Margin="6 0 0 0" />
        </BoxContainer>
        <PanelContainer StyleClasses="LowDivider" Margin="0 0 0 8" />
        <BoxContainer Orientation="Horizontal" Margin="0 0 0 8">
            <Label Text="{Loc 'criminal-records-console-status'}" FontColorOverride="DarkGray" />
            <Label Text=":" FontColorOverride="DarkGray" />
            <AnimatedTextureRect Name="StatusIcon" Margin="8 0" />
            <OptionButton Name="StatusOption" MinWidth="150" HorizontalExpand="True" />
        </BoxContainer>
        <RichTextLabel Name="StatusReason" Visible="False" MaxWidth="360" />
    </BoxContainer>
</controls:FancyWindow>
